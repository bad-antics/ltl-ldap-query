// ltl-ldap-query/main.ltl
//
// LDAP v3 search client with BER encode and simple bind.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ldap-query v0.1.0")
    io::println("LDAP v3 search client with BER encode and simple bind.")
    0
}
