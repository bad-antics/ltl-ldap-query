// examples/demo.ltl — runnable demo for ltl_ldap_query

import std::io
use ltl_ldap_query as lib

fn main() {
    io::println("ltl_ldap_query demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}